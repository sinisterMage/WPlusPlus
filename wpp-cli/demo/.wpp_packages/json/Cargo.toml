[package]
name = "wpp-json-ffi"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[workspace]
# This is a standalone library, not part of W++ workspace

[dependencies]
serde_json = "1.0"
libc = "0.2"

funcy worker() {
    let counter = useThreadState(0)
    while (counter < 3) {
        counter = counter + 1
    }
    print(counter)
}

funcy bgTask() {
    print("Running in background...")
}

useThread(worker)
useThread(bgTask, true)
print("Main continues!")
